<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        /* Basic Reset */
        body, table, td {
            margin: 0;
            padding: 0;
            border: 0;
            font-family: Arial, sans-serif;
        }
        img {
            border: none;
            -ms-interpolation-mode: bicubic;
        }

        /* Container */
        .email-container {
            width: 100%;
            max-width: 600px;
            margin: auto;
            background-color: #f4f4f4;
            border-radius: 8px;
            overflow: hidden;
        }

        /* Header */
        .email-header {
            background-color: #5337b1; 
            padding: 20px;
            text-align: center;
            color: white;
        }

        /* Body */
        .email-body {
            padding: 20px;
            background-color: white;
        }

        /* Footer */
        .email-footer {
            padding: 10px;
            text-align: center;
            font-size: 12px;
            color: #888;
        }

        /* Button Style */
        .verify-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #5337b1; 
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 10px;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .email-container {
                width: 100% !important; 
            }
        }
    </style>
</head>
<body>

    <div class="email-container">
        <div class="email-header">
            <h1>Welcome to Our Service!</h1>
        </div>
        <div class="email-body">
            <h2>Hello, {{userName}}!</h2>
            <p>Thank you for verifying your email address. We are excited to have you on board!</p>
            <p>To get started, please click the button below:</p>
            <a href="{{verificationToken}}" class="verify-button">Get Started</a> 
        </div>
        <div class="email-footer">
            <p>All rights reserved.</p>
        </div>
    </div>

</body>
</html>
