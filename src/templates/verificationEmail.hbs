
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        /* Basic Reset */
        body, table, td {
            margin: 0;
            padding: 0;
            border: 0;
            font-family: Arial, sans-serif;
        }
        img {
            border: none;
            -ms-interpolation-mode: bicubic;
        }

        /* Container */
        .email-container {
            width: 100%;
            max-width: 600px;
            margin: auto;
            background-color: #f4f4f4;
            border-radius: 8px;
            overflow: hidden;
        }

        /* Header */
        .email-header {
            background-color: #5337b1; 
            padding: 20px;
            text-align: center;
            color: white;
        }

        /* Body */
        .email-body {
            padding: 20px;
            background-color: white;
        }

        /* Footer */
        .email-footer {
            padding: 10px;
            text-align: center;
            font-size: 12px;
            color: #888;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .email-container {
                width: 100% !important; 
            }
        }
    </style>
</head>
<body>

    <div class="email-container">
        <div class="email-header">
            <h1>Email Verification</h1>
        </div>
        <div class="email-body">
            <h2>Hello, {{userName}}!</h2>
            <p>Thank you for registering with us. To complete your registration, please verify your email address by using the below token</p>
            <p>Token will be expired in 10 min.</p>
            <a href="{{verificationToken}}">{{verificationToken}}</a> 
            <p>If you did not create an account, no further action is required.</p>
        </div>
        <div class="email-footer">
            <p>All rights reserved.</p>
        </div>
    </div>

</body>
</html>
